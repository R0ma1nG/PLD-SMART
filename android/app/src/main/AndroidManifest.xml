<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.h4413.recyclyon">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Connection.ConnectionActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Connection.InscriptionActivity"
            android:label="@string/inscription"
            android:parentActivityName=".Connection.ConnectionActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Connection.CGUActivity"
            android:label="@string/app_name"
            android:parentActivityName=".Connection.ChooseAssociationActivity" />
        <activity
            android:name=".Connection.ForgotPwdActivity"
            android:parentActivityName=".Connection.ConnectionActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".HomeActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Connection.ChooseAssociationActivity"
            android:screenOrientation="portrait"
            android:parentActivityName=".Connection.InscriptionActivity"/>
    </application>

</manifest>